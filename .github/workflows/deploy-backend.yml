name: Deploy Backend to Coolify

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coolify deploy
        run: curl --fail -X GET "${{ secrets.COOLIFY_WEBHOOK_URL }}"
